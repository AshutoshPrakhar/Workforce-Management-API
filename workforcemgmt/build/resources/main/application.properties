# Spring Boot default server port (optional)
server.port=8080

# Logging level (optional but helpful)
logging.level.org.springframework=INFO
logging.level.com.company.workforcemgmt=DEBUG

# App-specific properties (optional but useful for future configs)
app.name=Workforce Management API
